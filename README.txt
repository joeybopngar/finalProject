In Order to execute this program got to the two comments in the main
that say to input words. change the words in the quotes to whaterver u wish
to search for.

First function is trivial
second is trigram algorithim